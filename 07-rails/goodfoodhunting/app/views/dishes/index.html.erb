
<h2>list</h2>

<% @dishes.each do |dish| %>
  <%= render partial: 'dish_card', locals: { dish: dish} %>
<% end %>

<script>
  $stars = $('.star');

  $stars.on('click', function(event){

    var $card = $(event.target).closest('.ui-card');
    var dishId = $card.attr('data-id');
    $.ajax({
      url: '/api/likes',
      method: 'post',
      data: { dish_id: dishId }
    }).done(function(data){
      $card.find('.likes-count').text(data.likes_count);// updated the like count span relative to where we are
    })



    // var $myTarget = $(event.target)
    //   .closest('.ui-card') //find closes parent / up
    //   .find('.likes-count')// down
    //
    //
    // var oldText = $myTarget.text();
    // var oldCount = Number(oldText);
    // var newCount = oldCount + 1;
    //
    // $myTarget.text(newCount);
  });

  //$.getJSON()

  $.ajax({
    url: '/api/likes',
    data: { dish_id: 11}, //hard code dish id 5
    method: 'post'
  }).done(function(data){
    console.log(data)
  });




</script>
